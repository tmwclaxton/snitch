# Production stack for Snitch
# Usage: docker compose -p snitch -f compose.prod.yaml --env-file .env up -d

services:
  app:
    image: ghcr.io/tmwclaxton/snitch:latest
    stop_grace_period: 120s
    env_file: .env
    environment:
      WWWUSER: "1337"
      WWWGROUP: "1000"
      DB_HOST: postgres
      REDIS_HOST: redis
    ports:
      - "8095:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - snitch
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/up"]
      retries: 3
      timeout: 5s
      start_period: 40s
    restart: unless-stopped
    volumes:
      - snitch-storage:/var/www/html/storage

  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - snitch-postgres:/var/lib/postgresql/data
    networks:
      - snitch
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}"]
      retries: 10
      timeout: 10s
      start_period: 30s
    restart: unless-stopped

  redis:
    image: redis:alpine
    volumes:
      - snitch-redis:/data
    networks:
      - snitch
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      retries: 3
      timeout: 5s
    restart: unless-stopped

networks:
  snitch:
    driver: bridge

volumes:
  snitch-postgres:
    driver: local
  snitch-redis:
    driver: local
  snitch-storage:
    driver: local
